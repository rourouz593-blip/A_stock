# Shared core

Only code used by multiple agent packages belongs here: repository paths, file contracts, agent discovery, artifact persistence, environment loading, and common CLI helpers.

Agent-specific behavior belongs under `agents/<package>/`; do not add ownership manifests.
