# 02 · 组件如何咬合

## 一、一句话区分六层

| 层 | 回答的问题 | 放什么 | 不放什么 |
|---|---|---|---|
| Agent | 我是谁 | 职责、输入输出边界 | 算法、代码 |
| Skill | 这类事怎么做 | 方法论、清单、模板 | 具体某次运行的数据 |
| Tool | 我怎么动手 | CLI 薄封装 | 业务逻辑、编排 |
| Contract | 我们传什么 | 字段结构与约束 | 业务含义的解释 |
| Memory | 上次学到什么 | 踩坑、决策、运行摘要 | 通用方法论 |
| Workspace | 这次发生了什么 | 中间产物与日志 | 需要长期保留的东西 |

## 二、最容易混的三组边界

### Skill vs Memory

- Skill：**通用**方法论 —— "读财报要看经营性现金流与净利润的比值"
- Memory：**本项目**经验 —— "某数据源的现金流字段单位是万元，取回来要乘 10000"

判断法：**换一个项目还成立吗？** 成立 → skill；不成立 → memory。

### Skill vs Tool

- Skill：需要**判断**的部分 → 给模型看
- Tool：需要**精确**的部分 → 让代码算

判断法：**同样的输入，要求每次输出完全一样吗？** 要 → tool；不要 → skill。

### Tool vs Script

- Tool：接口层，负责参数解析与 JSON 输入输出，**很薄**
- Script：实现层，负责真正的逻辑，**可单测**

判断法：这段代码**能不能脱离 agent 单独测试**？能 → 放 script。

## 三、依赖方向（不许反向）

```
agents  ──调用──▶  tools  ──调用──▶  scripts  ──访问──▶  外部世界
   │                                                     
   ├──加载──▶  skills        （只读方法论）
   ├──读写──▶  workspace     （受 schemas 约束）
   └──读写──▶  memory        （跨 run 保留）
```

**反向依赖是设计事故：**
- ❌ `scripts/` 里写 prompt 或调用模型
- ❌ `skills/` 里写死某次运行的数据
- ❌ `tools/` 里做多步编排（编排是 orchestrator 的活）

## 四、加一个新东西时，改哪几处

| 新增什么 | 需要同步改动的地方 |
|---|---|
| 一个 agent | `agents/*.md` → `schemas/*.json` → orchestrator 调度表 → `AGENTS.md` 流程图 |
| 一个 skill | `skills/<name>/SKILL.md` → 在引用它的 agent 的 `skills:` 里登记 |
| 一个 tool | `tools/<name>.py` → `tool_manifest.yaml` → agent 的 `tools:` 里登记 |
| 一个字段 | `schemas/*.json` → agent 定义的输出说明 → 示例 run → 跑 validate |

**漏改任何一处，系统都会以"不报错但结果不对"的方式失败。**

## 练习

1. `a-share-market-basics` 被四个 agent 引用。如果把它的内容直接抄进四个 agent 定义里，
   会带来什么问题？（提示：不只是重复）
2. `compute_indicators` 为什么是 tool 而不是写进 `technical-analyst` 的 prompt？
3. "某数据源在北京时间 15:30 前不更新当日收盘价" —— 这条应该放 skill 还是 memory？
