# ── 核心 ───────────────────────────────────────────────
pydantic>=2.6          # 数据契约校验
pyyaml>=6.0            # 配置与 frontmatter 解析
jsonschema>=4.21       # 文件契约校验

# ── 数据处理 ────────────────────────────────────────────
pandas>=2.2
numpy>=1.26

# ── 数据源 SDK（TODO(datasource): 确定后取消注释） ────────
# akshare>=1.12        # 免费，覆盖广，稳定性一般
# tushare>=1.4         # 需积分，财务数据质量高
# baostock>=0.8        # 免费，历史行情
# efinance>=0.5        # 东方财富，资金流/情绪

# ── 技术指标（TODO(strategy): 确定后取消注释） ────────────
# ta-lib>=0.4          # 需先装 C 库
# pandas-ta>=0.3

# ── 测试 ───────────────────────────────────────────────
pytest>=8.0
