# ── 数据源 ─────────────────────────────────────────────
akshare>=1.16          # A 股全部数据来源，免费无需 token

# ── 数据处理 ────────────────────────────────────────────
pandas>=2.0
numpy>=1.24

# ── 契约与配置 ──────────────────────────────────────────
jsonschema>=4.21       # 文件契约校验（tools/validate_artifact.py）
pyyaml>=6.0            # 配置与 frontmatter 解析

# ── 测试 ───────────────────────────────────────────────
pytest>=8.0

# 注：报告的 HTML 仪表盘由 tools/render_report.py 用纯标准库生成，
# 不依赖 jinja2 / plotly / echarts，产出是单文件、可离线打开。
